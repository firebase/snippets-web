{
  "name": "auth",
  "version": "1.0.0",
  "scripts": {
    "compile": "cp ../tsconfig.json.template ./tsconfig.json && tsc"
  },
  "license": "Apache 2.0",
  "dependencies": {
    "firebase": "^8.10.0",
    "firebaseui": "^5.0.0"
  }
}
